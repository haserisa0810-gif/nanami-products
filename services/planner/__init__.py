"""Vendored Astrology Transit Planner generator.

Source of truth lives in the standalone sticker repo
(annual_transit_planner/). Re-vendor by copying the four modules and
re-applying the package-relative import fixes in generate_planner.py and
build_personal_planner.py. See services/planner_export.py for the app-facing
entry point.
"""
