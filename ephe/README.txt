Place Swiss Ephemeris data files here when using SWIEPH mode.
Examples: seas_18.se1, sefstars.txt, sepl_18.se1, semo_18.se1 ...

This project now falls back to Moshier ephemeris automatically if these files are missing,
so the app can still run without this directory. For higher fidelity, deploy the real ephemeris files here.
